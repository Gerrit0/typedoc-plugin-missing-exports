/**
 * This is a package where all referenced symbols are exported.
 * No extra "(internals)" namespace should be created by the plugin.
 * @module
 */

/**
 * Foo docs
 */
export function foo() {
	return 123;
}
